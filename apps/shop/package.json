{
  "name": "@react-mono/shop",
  "version": "0.0.1",
  "private": true,
  "nx": {
    "projectType": "application",
    "tags": ["scope:shop"],
    "targets": {
      "serve": {
        "dependsOn": ["@react-mono/api:serve"]
      },
      "dev": {
        "dependsOn": ["@react-mono/api:serve"]
      },
      "preview": {
        "dependsOn": ["build", "@react-mono/api:serve"]
      }
    }
  },
  "devDependencies": {
    "@react-mono/models": "*",
    "@react-mono/shop-data": "*",
    "@react-mono/shop-feature-product-detail": "*",
    "@react-mono/shop-feature-products": "*",
    "@react-mono/shop-shared-ui": "*"
  }
}
